# Forex Bot — CONTEXT v2.8
**Дата обновления:** 2026-08-29 (вечер, +live-пакет)

## ✅ СЕССИЯ 29.08 ВЕЧЕР — ВИТРИНА fxbot.space ПОКАЗЫВАЕТ ГИБРИД (читать первым!)

**Итог: публичный сайт переведён с сетки на гибрид и работает.**
https://fxbot.space показывает живые цифры: **PnL +$52.84, WR 83.8%, 74 закрытых
сделок**, инструменты XAUUSD.s/NAS100.s/GER40.s, плашка «ДЕМО-СЧЁТ», русский текст
цел. Данные: `/api/hybrid_summary`, `/api/hybrid_pairs`. Задеплоено на VPS ~18:56,
бэкап прежних версий: `C:\forexbot\backup\webfix_20260829_185637`.

⚠️ ЭТО ОТДЕЛЬНАЯ ПРАВКА от утренней сессии (см. ниже): утром чинились /status,
канал и брендинг; вечером — сама витрина fxbot.space. После утренней сессии сайт
всё ещё показывал сетку (−$31.87, halted) и пустые «—».

### Настоящая причина пустой витрины (баг существовал ДО всех правок)
JS в `user.html` обращался к `document.getElementById('status-pill')`, но элемента
с таким id в разметке **не было** → TypeError → вся функция `load()` падала в
`catch` → все карточки оставались «—». Поэтому сайт был пустым ещё при живом
`/api/summary`. Вторая причина: витрина читала сетку, а не гибрида. Оба исправлены.

### Что сделано в коде
- `webapp.py`: публичные эндпоинты **`/api/hybrid_summary`** (форма ответа как у
  /api/summary, но stats/state от гибрида, `per_symbol`, `account_mode`) и
  **`/api/hybrid_pairs`** (сделки гибрида в форме, которую ждёт страница). Хелпер
  `_hybrid_state()` читает `state_hybrid.json`.
- `user.html`: добавлен элемент `status-pill` (фикс падения JS); fetch переведён
  на новые эндпоинты; сеточные подписи заменены («Диапазон сетки»→Winrate,
  «Золото в позициях»→«PnL сегодня», «История пар»→«История сделок»,
  «Затраты»→«Причина»); в открытых сделках свой TP (не buy+tp_usd от сетки);
  лента событий фильтруется по kind=hybrid/halt/resume; плашка счёта
  `mode-pill` = ДЕМО/РЕАЛЬНЫЙ по `account_mode`.
- **Унификация режима (важно!):** витрина теперь читает `config.TRADING_MODE`
  (та же переменная, что у канала и /status с утренней сессии) → `account_mode`
  в API. Переход на реал = ОДНА строка `TRADING_MODE=real` в `.env` VPS + рестарт
  webapp и main. Промежуточная `ACCOUNT_MODE` из ранней версии правки УДАЛЕНА —
  не плодить две переменные режима.

### Деплой на VPS (29.08 ~18:56, успешно)
Скрипт `_server/webfix_vps.ps1` (генератор `_server/gen_webfix.py`; в git не
попадает — `_server/` в .gitignore). Файлы передаются **gzip+base64 + контроль
SHA256**: при обрыве вставки скрипт падает с «checksum mismatch» и ничего не
пишет. Шаги: бэкап → запись файлов → `py_compile` → рестарт ТОЛЬКО
процесса-владельца порта 8181 (PID из `Get-NetTCPConnection`; НЕ taskkill /IM
python.exe — на VPS живут гибрид и main!) → запуск `python -m backend.webapp` →
проверка эндпоинта. Итог: `[ok] hybrid PnL=52.84 winrate=83.8% closed=74 mode=demo`.

### ⚠️ Ошибки этой сессии (зафиксировано, чтобы не повторять)
1. **PS-скрипты падали на PS 5.1** (Server 2016): оператор `?.Id` не поддержан;
   незакрытые скобки; лишний `)`; сломанные here-строки.
2. **Неверный путь:** `E:\AI\AI_folder\forexbot` — на VPS проекта нет (там
   `C:\forexbot`, диска E: нет вовсе) → «Cannot find drive 'E'».
3. **Кодировка сломана моими же командами:** `Get-Content`/`Set-Content` без
   encoding перечитали UTF-8 как ANSI → кракозябры «вљЎ Gibrid-bot» по всему
   сайту. Откат + повторная поставка целыми файлами.
4. **Regex-замена в PS вставила в JS литеральные `\(`** — fetch сломался,
   страница легла полностью.
5. **Вставка эндпоинта по кириллическому маркеру не совпала** (кодировка),
   ручное добавление в конец webapp.py дало неверный импорт
   (`from fastapi import JSONResponse`; верно — `fastapi.responses`, уже есть
   наверху файла).
6. **Несколько неудачных итераций PS-скриптов подряд** — юзер явно попросил:
   «я не прогер, скрипт должен делать всё сам». Вывод: давать ОДИН
   self-contained скрипт, ничего не требующий от юзера, кроме вставки.
7. **Правила на будущее:** (а) НЕ править UTF-8 файлы на VPS через PS
   Get-Content/Set-Content — только передача целых файлов (base64/gzip+SHA256)
   или python с `io.open(encoding='utf-8')`; (б) PS-скрипты для VPS — ASCII-only,
   без `?.`, пути проверять заранее; (в) web-правки сначала проверять локально
   (uvicorn :8199 против реальной bot.db), потом деплой; (г) рестарт веб-процесса
   — по PID владельца порта 8181, никогда не трогать другие python-процессы.

### Конфликт с параллельной утренней сессией (урок)
В тот же день параллельно шла утренняя сессия (коммиты 25f7e04, f9a9b20, ff2cf6c:
Gibrid-bot ребрендинг, /status на гибрид, TRADING_MODE, трансляция в канал).
Обе сессии независимо ввели свой флаг режима: утро — `TRADING_MODE`, вечер —
`ACCOUNT_MODE`. Обнаружено при обновлении контекста; унифицировано на
`TRADING_MODE`, `ACCOUNT_MODE` удалена. Мораль: перед вводом новой глобальной
переменной — grep по проекту на предмет существующей.

### Брокерский вопрос (из чата 29.08)
Юзер открыл/планирует счёт на `direct.fxpro-start.group`. Это **не** официальный
домен FxPro (fxpro.com) — похоже на клон; до депозита проверить: имя сервера в
MT5, регуляцию, вывод средств. Технически бот не привязан к брокеру (любой MT5),
но символы PU Prime с суффиксом `.s` зашиты в `HYBRID_SYMBOLS`. При смене брокера:
демо у него → узнать имена символов → правка HYBRID_SYMBOLS → `main_hybrid risk`
→ 1–2 дня на их демо. Спреды/комиссии иные → пересчитать ожидания.

### «Почему нет сделок 29.08» — норма, не сбой
Суббота: рынок закрыт, гибрид в `weekend_flat` (запроектировано). Последняя сделка —
пятница 19:53 UTC (NAS100.s TP +$0.24). Возобновление — воскресенье ~22:00 UTC,
перезапуск не нужен. Snapshot пишется (процесс жив). Пятничное
`skipped: XAUUSD.s риск 14.7% > 10%` — риск-фильтр по волатильности золота; если
повторится в понедельник — смотреть параметры SL/волатильность.

### 🔴 Открытые вопросы (актуально на вечер 29.08)
1. **Безопасность (блокер live, НЕ сделано):** перевыпустить Telegram-токен и
   GitHub-токен, удалить release+тег `vps-data` (тег проверен 29.08 — ещё в
   репо, там bot.db с секретами), сменить пароль Administrator VPS (светился
   в чате, в т.ч. в этой сессии) и root крипто-VPS.
2. **Команды `/stop`/`/start` — риск до live:** открытый доступ (любой написавший
   получает команды) + `/stop` делает `taskkill terminal64.exe` — убивает ОБЩИЙ
   терминал гибрида; `/start` возобновляет СЕТКУ на общем счёте (противоречит
   решению «на live только гибрид»). До live: ограничить команды по chat_id
   владельца, /stop не должен трогать терминал.
3. **Витрина live-режима:** блок «Реальные счета + копитрейдинг — Скоро»
   активировать при запуске копира; при live: `TRADING_MODE=real` в `.env` VPS
   + рестарт webapp и main.
4. **Деплой unified-версии на VPS:** вечерний деплой 18:56 ставил webapp.py со
   СТАРОЙ переменной `ACCOUNT_MODE` (наследие правки до унификации). Это работает
   и безопасно: на витрине корректно «ДЕМО-СЧЁТ» (дефолт). Но локальная версия
   уже унифицирована на `TRADING_MODE`; при ближайшем деплое поставить
   unified-версию (скрипт `_server/webfix_vps.ps1` пересобран, в буфере).
   После этого `ACCOUNT_MODE` нигде не останется.
5. **Проверка сигналов в канале:** формат `[ДЕМО] 🟢 ГИБРИД ВХОД …` ещё не виден
   вживую (рынок закрыт) — глянуть в воскресенье вечером/понедельник.

### 📦 LIVE-ПАКЕТ ГОТОВ (папка `live/`, подготовлен 29.08, НЕ применён)

Юзер дал данные реального счёта PU Prime: **логин 29399422, сервер PUPrime-Live,
депозит $150**. По его просьбе правки подготовлены заранее, локально, БЕЗ остановки
демо — чтобы к моменту ввода данных в терминал код был готов.

⚠️ Пароль счёта прошёл через чат (как ранее токен бота и пароль VPS) — **сменить
в кабинете PU Prime** до старта. Пароль в MT5 вводится руками (терминал не
переносит сохранённые пароли между машинами).

| Файл в `live/` | Что делает | Проверено |
|---|---|---|
| `README.md` | чек-лист перехода по этапам 0-5 + обоснование решений | — |
| `check_live.py` | подключение (счёт/сервер/баланс/trade_allowed), фактические имена символов у брокера + поиск похожих при несовпадении, риск-таблица гибрида. Только чтение | компилируется; на live-счёте не гонялся (нет доступа к терминалу) |
| `split_db.py` | демо-история → `data/archive/bot_demo_<дата>.db`, рабочая БД чистая (schema сохраняется). Сверяет архив с исходной БД построчно ДО очистки | dry-run на реальной БД (218 сделок/40 пар/591 событий/3384 снимка) + полный прогон на копии в /tmp: архив создан, таблицы обнулены, 40 КБ |
| `patch_tg_owner.py` | `/stop`,`/start` только владельцу; `/stop` при `TRADING_MODE=real` НЕ гасит терминал; `/help` скрывает управляющие команды от посторонних | applied на 3 изолированных копиях; идемпотентность (повторный запуск → skip); симуляция всех 4 команд × владелец/посторонний × demo/real |
| `env_live.template` | образец блока `.env` (логин/сервер/пароль, `HYBRID_TEST_BALANCE=150`, `TRADING_MODE=real`, `TELEGRAM_OWNER_ID`, задел под копир) | — |

**Результат симуляции команд (важно для приёмки):**
```
demo: владелец /stop → halt + ТЕРМИНАЛ УБИТ (как раньше)
real: владелец /stop → halt, терминал ОСТАВЛЕН включённым  ← главный фикс
оба:  посторонний /stop,/start → «🔒 только владельцу», halt НЕ происходит
оба:  посторонний /status,/help → отвечают (витрина для подписчиков цела)
```

**Ключевые решения live-пакета:**
1. **Чистая БД обязательна ДО первой live-сделки.** В `scalp_trades` нет колонки
   счёта/режима (проверено по схеме) → live-сделки легли бы к 74 демо-сделкам,
   и витрина «проверенная статистика» показывала бы смесь. Альтернативу «добавить
   колонку account и фильтровать везде» отверг: правок больше (storage + все
   *_stats + витрина + Excel), выгода только в едином файле — чего не нужно.
2. **Риск-база = 150, а не 100.** Гибрид считает лимиты от `HYBRID_TEST_BALANCE`;
   при базе 100 и депозите $150 дневной стоп сработал бы на $8 вместо $12.
3. **Один терминал = один счёт.** При входе в live демо-торговля гибрида
   прекращается. Для параллельного демо нужен 2-й терминал в отдельной папке —
   для live-теста признано лишним.
4. **Символы проверяем до старта:** на live суффиксы часто иные, чем `.s` на демо;
   на $150 золото может не пройти риск-фильтр (на демо при базе $100 уже
   отсекалось: `skipped: XAUUSD.s риск 14.7% > 10%`).

### Дорожная карта (актуальная, вечер 29.08)
1. **Security-пакет — блокер до любых реальных денег:** токен бота, GitHub-токен,
   release+тег `vps-data`, пароли обоих VPS, пароль счёта 29399422.
2. Деплой unified-версии webapp на VPS (скрипт `_server/webfix_vps.ps1` готов).
3. **Переход на live по чек-листу `live/README.md`** (этапы 0-4): вход в терминал →
   `check_live.py` → `split_db.py` → `.env` по `env_live.template` →
   `patch_tg_owner.py` → рестарт задач → первый день ручной контроль.
4. Проверить сигналы гибрида в канале при открытии рынка (вс–пн): `[ДЕМО]`/`[LIVE]`.
5. Копир: 2-й реальный счёт + 2-й терминал в ОТДЕЛЬНОЙ папке → `COPIER_DRY_RUN=1`
   ≥1 день → сверка `copy_trades` → `COPIER_DRY_RUN=0` (этап 5 в `live/README.md`).
6. Решение по брокеру: остаёмся на PU Prime (счёт уже есть) или FxPro — но домен
   `direct.fxpro-start.group` НЕ официальный (fxpro.com), проверять до депозита.

---

## ✅ СЕССИЯ 29.08 УТРО — РЕЛЕЙ + РЕБРЕНДИНГ Gibrid-bot + ТРАНСЛЯЦИЯ ГИБРИДА

Сессия закрыла два больших блока: (1) добила Telegram-релей до рабочего состояния
end-to-end; (2) переключила «витрину» (бот `/status` + канал + сайты) со старой
сетки/grid на **гибрид** — с ребрендингом в **Gibrid-bot** и заделом под будущий
переход на реальные счета + копитрейдинг.

### 1. Telegram-релей — РАБОТАЕТ (детали в разделе ниже «СЕССИЯ 28.08 НОЧЬ»)
- Релей `https://trade.aliterra.space/tgrelay-fx99` на крипто-VPS (Caddy) — проверен
  end-to-end: getMe / getWebhookInfo / sendMessage → все 200, `pending_update_count=0`,
  409 Conflict отсутствует.
- Проверено, что Caddy крипто-VPS обслуживает РЕАЛЬНО (не устаревший handoff):
  **ai / trade / promo / unidev(+www) / parser.unidev.space** — все целы, релей
  добавлен отдельным `handle /tgrelay-fx99/*` внутри блока trade, соседи не задеты.
  Бэкап `/etc/caddy/Caddyfile.bak.1787944991`.

### 2. Ребрендинг Grid Bot → Gibrid-bot + переключение витрины на гибрид
**Проблема, которую решали:** `/status` в боте и публичный сайт показывали
процесс `main` (сетка/grid, запущен halted — «PnL −31.87, Halted:True»), тогда как
реально торгует и зарабатывает **гибрид** (`main_hybrid`, +$52.84 WR 84%). Витрина
показывала «мёртвый» бот. Плюс сайты имели старый бренд «XAUUSD Grid Bot».

**Что сделано (коммиты 25f7e04 + f9a9b20 в main):**

| Файл | Изменение |
|------|-----------|
| `config.py` | + `TRADING_MODE` (demo\|real), `TRADE_MODE_TAG` ([ДЕМО]/[LIVE]); + `HYBRID_CHANNEL_SIGNALS` / `HYBRID_SIGNAL_FILLS` / `HYBRID_SIGNAL_CLOSES` (все дефолт 1) |
| `notifier.py` | + `send_signal(text)` — шлёт в канал с тегом режима, уважает HYBRID_CHANNEL_SIGNALS; `/status` ПЕРЕПИСАН на `storage.hybrid_stats()` (гибрид, а не сетка) + строка режима + «💰 Реальные счета — скоро» в demo; `/help` → бренд Gibrid-bot |
| `hybrid_engine.py` | в `sync()`: на FILL → `send_signal("🟢/🔴 ГИБРИД ВХОД …")`, на CLOSED → `send_signal("✅/🔻 ГИБРИД ЗАКРЫТ … PnL/WR/итого")` |
| `static/user.html` | бренд → **Gibrid-bot** (title/логотип «⚡ Gibrid-bot»/h1/секция); + 2 блока витрины: «📊 Демо-результаты (Активно)» и «💰 Реальные счета + копитрейдинг (Скоро)» |
| `static/admin.html` | title + h1 → Gibrid-bot |

**Концепция режима (важно на будущее):** одна переменная `.env` `TRADING_MODE=demo|real`
управляет всей витриной. Сейчас `demo` → сигналы/статус/сайт помечены `[ДЕМО]`,
на сайте активен блок «Демо». Переход на реал = поменять на `real` + рестарт →
тег станет `[LIVE]`, статус/канал начнут писать про реальные счета. Блок
«Реальные счета + копитрейдинг» на сайте уже зарезервирован (placeholder «Скоро»).

**Решение по трансляции:** слать в канал И входы, И закрытия (по указанию юзера;
изначально предлагалось только закрытия из-за 25-40 сделок/день у гибрида).

### 3. Деплой на VPS (через RDP-буфер, без токенов)
- Способ: файлы → base64 → `Set-Clipboard` на ПК → Ctrl+V в PowerShell на VPS →
  `[IO.File]::WriteAllBytes(...FromBase64String(...))`. Обходит и кириллицу
  Server 2016, и необходимость светить GitHub-токен. Работает надёжно.
- `.env` VPS: добавлен `TRADING_MODE=demo` (стр.13), `TELEGRAM_API_BASE` уже был (стр.11).
- **Грабли деплоя, которые ловили:**
  1. `schtasks /Run /TN fxbot-web` НЕ перезапустил webapp (PID остались вчерашние 22:02)
     → сайт отдавал старый HTML. Лечится: убить python-процессы webapp вручную
     (`Stop-Process`) → `schtasks /Run`. Проверка: `Invoke-WebRequest localhost:8181`
     → `-match 'Gibrid-bot'` = True.
  2. **`/status` падал молча** — `KeyError 'closed_pairs'`: `hybrid_stats()` возвращает
     ключ **`closed_trades`**, а не `closed_pairs` (как у сетки). В логе было
     `telegram poll failed: 'closed_pairs'`. Фикс f9a9b20 → задеплоен → работает.
  3. `Get-NetTCPListener` НЕ существует на Server 2016 (PS 5.1 старый) — для проверки
     портов использовать `netstat`. Но сайт снаружи и так отдаёт HTTPS 200 (порты открыты).

### 4. Процессы на VPS — по одному, дублей НЕТ (проверено 10:42)
- **hybrid** (main_hybrid, торгует), **main** (/status + TG-команды), **webapp** (сайты).
- Каждый бот = пара python PID (venv-лаунчер `C:\forexbot\.venv` порождает
  `C:\Program Files\Python312\python.exe` как дочерний) — это НОРМА, не дубль.
- Рестарт `fxbot-telegram` поднимает `main` заново; чтобы не плодить дубль getUpdates
  (→409), при деплое сначала `Stop-Process` старого `backend.main run`, потом Run.

### 5. Проверено вживую юзером (11:09): «работает, сайт работает»
- `/status` отвечает, сайт fxbot.space открывается с брендом Gibrid-bot.
- ⚠️ НЕ проверено вживую (первый прогон): реальный формат сигналов гибрида в канале
  @forex_vip_first при следующей сделке — стоит глянуть `[ДЕМО] 🟢 ГИБРИД ВХОД …`.

### 🔴 Осталось ТОЛЬКО безопасность (secrets светились в чате/на VPS многократно)
1. **Перевыпустить токен бота** @Dim_forex_bot у @BotFather → `TELEGRAM_BOT_TOKEN`
   в `.env` VPS → рестарт `fxbot-telegram`. (Токен `8717937940:AAH…` светился в чате ОЧЕНЬ много раз.)
2. Перевыпустить GitHub-токен `ghp_…`
3. Удалить GitHub release `vps-data` (id 378694659) — bot.db + скрипты с секретами
4. Сменить пароли Administrator VPS (185.46.10.179) и root крипто-VPS (168.222.143.103)

### Ключевые адреса/факты сессии (шпаргалка)
- Форекс-VPS: `185.46.10.179`, Windows Server 2016, Administrator, проект `C:\forexbot`
- Крипто-VPS (релей): `168.222.143.103` = trade.aliterra.space, SSH root, plink с ПК,
  hostkey `SHA256:kTPrb01XLPu73Wwm45TIweNoMja2WroQnMRDblRi4e8`
- Релей URL: `https://trade.aliterra.space/tgrelay-fx99` (+ `/bot<token>/<method>`)
- Владелец chat_id: `789368186` (DMITRY / @LUXury_CEO)
- Локальный репозиторий разработки: `E:\AI\AI_folder\forexbot`, git `C:\Program Files\Git\bin\git.exe`
- Задачи автозапуска: fxbot-terminal / fxbot-hybrid / fxbot-telegram(держит main) / fxbot-web / fxbot-caddy

---

## ✅ СЕССИЯ 28.08 НОЧЬ — TELEGRAM ЧЕРЕЗ РЕЛЕЙ ЗАРАБОТАЛ

**Итог: блокер Telegram закрыт.** На новом VPS Telegram TCP-блокировался целиком
(даже connect не проходил, IP-пиннинг бесполезен). Решение — **релей через
крипто-VPS** — заведено и проверено end-to-end.

### Как устроен релей (РАБОТАЕТ)
- На крипто-VPS `168.222.143.103` (**trade.aliterra.space**, Caddy) добавлен маршрут
  внутри существующего блока `trade.aliterra.space { ... }`:
  ```
  handle /tgrelay-fx99/* {
      uri strip_prefix /tgrelay-fx99
      reverse_proxy 149.154.167.220:443 {
          header_up Host api.telegram.org
          transport http { tls; tls_insecure_skip_verify }
      }
  }
  ```
  Порядок: специфичный `/tgrelay-fx99/*` идёт **перед** `handle /api/*` и catch-all
  корнем grid-bot'а — соседи не задеты. `caddy validate` = Valid. Бэкап:
  `/etc/caddy/Caddyfile.bak.1787944991` (перед правкой, 22:23). **Diff подтвердил:
  добавлен только блок релея, больше ничего не тронуто.**
- ⚠️ Caddy крипто-VPS обслуживает НЕ web3/character/piper (устаревший handoff), а
  РЕАЛЬНО: **ai.aliterra.space, trade.aliterra.space, promo.aliterra.space,
  unidev.space(+www), parser.unidev.space** — все целы после правки.

### Код forexbot (закоммичено b66e17f, в main)
- `config.py`: `TELEGRAM_API_BASE = os.getenv("TELEGRAM_API_BASE", "https://api.telegram.org")`
- `notifier.py`: и `_api()`, и `send_photo()` строят URL от `config.TELEGRAM_API_BASE`
  (а не хардкод api.telegram.org). IP-пиннинг `_pin_telegram_dns()` остался, но не нужен.

### .env на боевом VPS (185.46.10.179, применено)
- Добавлено: `TELEGRAM_API_BASE=https://trade.aliterra.space/tgrelay-fx99`
- Закомментировано: `#TELEGRAM_API_IPS=...` (пиннинг больше не путь; релей основной)
- Код на VPS обновлён БЕЗ GitHub-токена: 2 файла переданы base64-командой через
  RDP-буфер (обход кириллицы Server 2016 + без светящихся секретов). `fxbot-telegram`
  перезапущен.

### Проверки (все ✅)
- `getMe` через релей → `{"ok":true,...username:"Dim_forex_bot"}`
- `getWebhookInfo` → `pending_update_count: 0` (было 4 — бот вычитал очередь, значит
  **409 Conflict исчез**, второго поллера нет после переустановки VPS)
- `sendMessage` владельцу (789368186) → HTTP 200, message_id 324 — доставлено

### Процессы на VPS вычищены (23:32)
Был дубль `backend.main run` (старт 22:02 и 23:13) → риск двойного getUpdates/409.
**Старый (22:02, PID 6600+1196) убит, оставлен свежий (23:13, PID 3728+2896).**
На VPS сейчас по одному: **hybrid (торгует, +$52.84 WR 84%), main (TG-команды), webapp.**
Примечание: каждый бот = пара python (venv-лаунчер порождает `Program Files\Python312`
как дочерний) — это НЕ дубль, а норма venv.

### 🔴 Осталась ТОЛЬКО безопасность (secrets светились в чате/на VPS)
1. **Перевыпустить токен бота** @Dim_forex_bot у @BotFather → обновить
   `TELEGRAM_BOT_TOKEN` в `.env` VPS → рестарт `fxbot-telegram`
2. Перевыпустить GitHub-токен `ghp_...`
3. Удалить GitHub release `vps-data` (id 378694659) — bot.db + скрипты с секретами
4. Сменить пароли Administrator VPS и root крипто-VPS

---

## 🔴 СЕССИЯ 28.08 ВЕЧЕР — ПЕРЕУСТАНОВКА VPS И ПОВТОРНЫЙ ДЕПЛОЙ

**Главное открытие: VPS 185.46.10.179 был ПЕРЕУСТАНОВЛЕН REG.RU ~28.08 10:09** —
чистая ОС (Cloudbase/openstack-образ), весь вчерашний деплой стёрт. Вероятно при
обработке тикета про порты. Поэтому порты «открылись», а всё остальное пропало.

### Развёрнуто заново (28.08 22:00, всё работает)
| Что | Статус |
|---|---|
| Python 3.12.10 + venv `C:\forexbot\.venv` | ✅ пакеты стоят, импорты ОК |
| Проект `C:\forexbot` | ✅ zipball с GitHub (ветка **main**, не master!) |
| Данные bot.db/state*.json/telegram.json | ✅ перенесены (GitHub release `vps-data`) |
| MT5 → **`C:\MetaTrader5`** (⚠️ путь изменился, задан в .env: MT5_TERMINAL_PATH) | ✅ подключён, 700158875, конфиг счёта подхвачен (папка данных BF9336D0...) |
| Firewall 80/443/8181 | ✅ |
| Caddy v2.11.4 `C:\caddy` | ✅ **https://fxbot.space и admin.fxbot.space РАБОТАЮТ (200, LE-серт)** |
| Автозапуск | ✅ 5 задач: fxbot-terminal/hybrid/web/telegram/caddy (SYSTEM, кроме терминала) |
| Гибрид | ✅ торгует: закрыл GER40 +$0.29, WR 84%, итого +$52.60 |

Деплой-скрипты: `_server/vps_setup_ascii.ps1` (полный, ASCII-only — Server 2016
ломает кириллицу в .ps1!) + `_server/vps_fix1.ps1`. Передача на VPS: GitHub release
asset + Invoke-WebRequest с токеном (проброс диска \\tsclient не работал).

### ✅ БЛОКЕР TELEGRAM ЗАКРЫТ (см. раздел «СЕССИЯ 28.08 НОЧЬ» вверху)
Была TCP-блокировка всех IP Telegram на новом VPS (жёстче пиннинга). Решено релеем
через крипто-VPS: `.env` VPS → `TELEGRAM_API_BASE=https://trade.aliterra.space/tgrelay-fx99`,
код читает базовый URL из config. getMe/sendMessage → 200, 409 исчез. Работает.

### ⚠️ Безопасность — сделать (secrets светились)
1. **Перевыпустить GitHub-токен ghp_42HU...** (светился в чате и на VPS)
2. **Удалить release `vps-data`** (id 378694659) — там bot.db и скрипты с секретами
3. Сменить пароль Administrator VPS и root крипто-VPS (светились в переписке)
4. **Telegram-токен бота светился многократно** — перевыпустить у @BotFather и обновить .env VPS

### Прочее
- AAAA-запись fxbot.space удалена юзером (была парковка REG.RU — IPv6 уводил мимо)
- 409 Conflict больше не воспроизводится (второй поллер исчез после переустановки)
- MT5-инсталлер и данные лежат в release vps-data (fxdata.zip, mt5setup.exe)

---

Торговый бот на MT5 (PU Prime, демо) + Telegram-канал сигналов + веб-панели.
**Пять ботов** в одном проекте, каждый — отдельный процесс, отдельная рамка в админке:
1. **Гибрид** (hybrid) — **лидер: +$47.55, WR 86.2%** — кандидат №1 на live
2. **Скальпер V2** (scalp) — чистый v2 +$3.65 (WR 76.5%, 17 сделок)
3. **Мульти-бот** (multi) — 6 инструментов, −$3.16, копит статистику
4. **Скальпер V3** (main_scalp3.py) — ML-скальпер, −$6.51, экспериментальный
5. **Сетка** (grid) — исторический лидер +$503.79 (35 закрытых пар)

Аналог крипто-бота trade.aliterra.space, портирован на форекс.

**Копитрейдинг** (copier_engine.py + main_copier.py) — написан и протестирован
в dry-run, **следующий шаг — подключение на сервере заказчика** (чек-лист в своём разделе).

---

## ⚠️ Секреты и приватность

Пароли/токены — только в **`.env`** (в git не попадает, см. `.gitignore`):
`MT5_PASSWORD`, `TELEGRAM_BOT_TOKEN`, `ADMIN_USER`, `ADMIN_PASS`, `WEB_PORT`, `PROFILE`.

**GitHub-репозиторий держать private до оплаты заказчиком.** Ссылку на код
не давать нигде: из публичной страницы `user.html` она убрана, в канале её нет.

---

## Инфраструктура

| Компонент | Детали |
|-----------|--------|
| **Продакшн-хост** | **VPS REG.RU `185.46.10.179`, Windows Server 2016, Administrator** (миграция 28.08, раздел ниже). ПК юзера — только разработка |
| Домен | **fxbot.space** (REG.RU): публичная страница + admin.fxbot.space (Caddy reverse-proxy, авто-HTTPS Let's Encrypt) |
| Терминал MT5 (прод) | `E:\AI\MetaTrader5\terminal64.exe` **на VPS** — путь повторяет локальный ради хэша папки данных `F0630FA0...` |
| Терминал MT5 (dev) | `E:\AI\MetaTrader5\terminal64.exe`, билд 6122, данные в `%APPDATA%\MetaQuotes\Terminal\F0630FA0BAF3EFE292639BFF14416EDC` |
| Счёт | PU Prime **DEMO**: login 700158875, сервер PUPrime-Demo, USD, плечо 1:500, старт $100 000 (на 28.08: $100 410.25) |
| Python (прод) | 3.12.10 `C:\Program Files\Python312`, venv `C:\forexbot\.venv`, тот же набор пакетов |
| Python (dev) | 3.12.9 (uv venv) в `.venv`, пакет `MetaTrader5` 5.0.6090 |
| Проект (прод) | `C:\forexbot` (без .git/.venv/логов; bot.db и state*.json скопированы 28.08) |
| GitHub | `aliter230880/forexbot` — **private до оплаты** |
| Публичная страница | http://localhost:8181 (статистика для подписчиков) |
| Админка | http://localhost:8181/admin (BasicAuth, логин/пароль в `.env`) |
| Telegram-бот | @Dim_forex_bot — команды + уведомления |
| Канал сигналов | **@forex_vip_first** (бот админ) |
| Владелец | chat_id 789368186, подписчиков 3 |

---

## Структура проекта

```
forexbot/
├── backend/
│   ├── config.py         # все настройки, профили, загрузка .env
│   ├── mt5_gateway.py    # MT5 API: ордера, позиции, счёт, SLTP, filling-фолбэки
│   ├── grid_engine.py    # GridBot: сетка, авто-сдвиг, тренд-фильтр, guard
│   ├── scalp_engine.py   # ScalpBot v2: H1-тренд + ADX + M5-откат + трейлинг
│   ├── multi_engine.py   # MultiBot: та же логика на 6 инструментах, риск-фильтр $100
│   ├── hybrid_engine.py  # HybridBot: лимитки сетки + жёсткий SL, лидер проекта
│   ├── storage.py        # SQLite: pairs / scalp_trades / events / snapshots / copy_trades
│   ├── notifier.py       # Telegram: канал, подписчики, команды, фото
│   ├── chartimg.py       # рендер графика зоны торговли (matplotlib) для канала
│   ├── webapp.py         # FastAPI: публичная страница + админка + API
│   ├── main.py           # раннер основного бота (профиль из .env)
│   ├── main_multi.py     # раннер мульти-бота (отдельный процесс)
│   ├── main_scalp3.py    # раннер Скальпера V3 — ML-скальпер (отдельный процесс)
│   ├── main_hybrid.py    # раннер гибрида (отдельный процесс)
│   ├── copier_engine.py  # движок копитрейдинга (зеркалирование мастеров)
│   ├── main_copier.py    # раннер копира: run / status
│   ├── report_excel.py   # Excel-отчёт: сводка / по дням / все сделки
│   └── static/
│       ├── user.html     # публичная «страница доверия»
│       └── admin.html    # админка: управление, статистика, рассылки
├── data/                 # bot.db, *.log (bot/multi/hybrid/kiro/copier), state*.json, telegram.json (не в git)
├── start_bot.ps1         # запуск всех 3 процессов detached (переживают закрытие сессии)
├── .env                  # секреты (не в git)
└── CONTEXT.md
```

---

## Запуск и управление

```powershell
# поднять всё (бот по профилю + мульти-бот + веб) одной командой:
powershell -ExecutionPolicy Bypass -File E:\AI\AI_folder\forexbot\start_bot.ps1
```

```bash
cd E:\AI\AI_folder\forexbot
.venv\Scripts\python.exe -m backend.main run          # основной бот (профиль из .env)
.venv\Scripts\python.exe -m backend.main status       # статус
.venv\Scripts\python.exe -m backend.main dry-run      # план сетки без ордеров
.venv\Scripts\python.exe -m backend.main stop-all     # всё закрыть + halt
.venv\Scripts\python.exe -m backend.main_multi run    # мульти-бот
.venv\Scripts\python.exe -m backend.main_multi risk   # таблица риска стопов по символам
.venv\Scripts\python.exe -m backend.webapp            # панели :8181
.venv\Scripts\python.exe -m backend.main_copier run   # копитрейдинг (DRY_RUN=1 по умолчанию)
.venv\Scripts\python.exe -m backend.report_excel      # Excel-отчёт → data/bots_report.xlsx
```

**Важно:** запускать через `start_bot.ps1`, а не фоном из Git Bash — фоновые процессы
из bash умирают вместе с сессией шелла (это была причина 90% случаев «бот сдох»).

---

## Стратегия 1: Сетка (grid_engine.py, PROFILE=STANDARD)

**Результат демо: +$503.79 за 4 дня, 35 закрытых пар.** Лучший PnL, но стартовал раньше остальных.

| Параметр | Значение |
|----------|----------|
| Инструмент | XAUUSD.s (золото) |
| Сетка | 15 buy-limit уровней ниже цены, шаг $4 |
| Лот | 0.02 (контракт 100 oz, маржа ~$18/уровень при 1:500) |
| Тейк пары | **серверный TP позиции** (TRADE_ACTION_SLTP) — работает при выключенном боте |

### Авто-механизмы
1. **Авто-сдвиг вверх** — цена > верх сетки + 1 шаг → пересборка вокруг цены (кулдаун 10 мин)
2. **Поддержание снизу** — сработал уровень → добавляется новый ниже нижнего
3. **Ремонт TP** — сбит/отсутствует TP → переустановка; если bid ≥ TP → закрытие по рынку
4. **Тренд-фильтр** — ≤ -3%/24ч (H1) → отмена buy и пауза; возврат при > -2%
5. **Margin guard** — стоп при -12% equity от watermark; margin level < 300% → закрыть худшую; -5%/день → стоп до след. дня
6. **Weekend flat** — пятница **23:00 UTC** всё закрыть (рынок PU Prime до 23:55); сброс по широкому окну рыночного времени (не привязан к моменту открытия)
7. **Аномалия-защита** — SELL-позиции с magic бота закрываются

### Пресет MICRO (PROFILE=MICRO) — для депозитов $100-150
5 уровней · шаг/TP $8 · лот 0.01 · тренд-пауза -2% · стоп сетки -15% · дневной лимит -5%.

---

## Стратегия 2: Скальпинг v2 (scalp_engine.py, PROFILE=SCALP) — АКТИВЕН

### История: v1 провалился
59 сделок, winrate 30.5%, **-$28.10**. Разбор причин по данным:
- **Шорты против тренда** — 36 сделок, WR 28%, **-$21.93** (почти весь убыток). Золото в аптренде, движок шортил на каждом касании EMA.
- **Вход во флэте** — не было фильтра силы тренда, «откат» часто оказывался разворотом.
- **Стоп $1 ≈ спред $0.35** — 35% стопа съедал спред, цена выбивала шумом. Правка на TP$3/SL$2 без отбора сигналов дала ещё хуже: -$24.38 на 47 сделках против +$0.24 на 10 старых.
- Лимит 60/день только ускорил слив.

### Логика v2 (проверена бэктестом)
**Вход только по тренду H1** (цена + EMA50 с наклоном) — шорты в аптренде отключены.
**ADX ≥ 22** — во флэте не торгуем. **Сигнал на M5** (не M1) — меньше шума.
Откат к EMA9 внутри тренда + RSI в рабочей зоне (45-72 лонг / 28-55 шорт).

| Параметр | Значение |
|---|---|
| SL / TP | ATR(M5)×1.2 / ×2.4 (риск 1:2), границы SL $1.5–5 |
| Трейлинг | при +$1.5 → SL в безубыток (+$0.3), далее шаг $1 |
| Лимиты | 25 сделок/день, 1 позиция за раз, кулдаун 5 мин |
| Окно | 07:00–19:00 UTC |
| Стоп-серия | 3 стопа подряд → пауза до следующего дня |

**Бэктест на 17.4 днях (5000 свечей M5, спред $0.35, с трейлингом):**
136 сделок, winrate 53.7%, +$113 на лоте 0.01, средняя +$0.83, макс. просадка $31, ~7.8 сделок/день.
Оговорка: бэктест на закрытых свечах оптимистичнее (нет проскальзывания), реальный WR ожидается на 5-10 п.п. ниже.

**Текущий статус (27.08):** чистый v2 — 17 сделок, WR 76.5%, **+$3.65** (торгует с 26.08).
⚠️ В `scalp_stats()` и админке версия НЕ фильтруется: рамка «Скальпер V2» показывает
v1+v2 вместе (76 сделок, −$24.45, WR 40.8%). Excel-отчёт разделяет: строки
«Скальпер V1+V2 (как в админке)» и «Скальпер V2 чистый (без v1)» — суждение о стратегии по второй.

---

## Стратегия 3: Гибрид (hybrid_engine.py, main_hybrid.py) — ЛИДЕР, кандидат на live

Лимитные входы сетки (не платим спред на догоне цены) + жёсткий SL скальпинга.
`research_hybrid.py` на 30 днях золота: 14-16 сделок/день, WR ~78%, PF 1.36
против PF 0.91 у чистого скальпинга — экономия на спреде переворачивает матожидание.

| Параметр | Значение |
|---|---|
| Инструменты | 3, max 3 позиции на символ |
| Вход | лимитки сетки по направлению тренда H1 |
| TP | адаптивный ≥10 спредов |
| Защита | жёсткий SL на позицию · пробой тренда H1 против нас → аварийный выход всей корзины · риск корзины ≤25% · дневной стоп 8% · флэт перед выходными |

Стресс-тест на падающем рынке дал маржин-колл у конфигураций без лимитов —
поэтому ограничения жёсткие. Ожидание: 25-40 сделок/день, 1.5-3%/день, просадка до 25%.

**Результат (27.08): 58 сделок, WR 86.2%, +$47.55; за сегодня +$17.35 на 30 сделках.**
Работает ровно как бэктест. **Решение по live-тесту $100: брать только гибрид**,
остальные боты остаются на демо добирать статистику.

---

## Стратегия 4: Мульти-символьный бот (multi_engine.py) — АКТИВЕН

Отдельный процесс, свой magic (`MULTI_MAGIC=20260825`), своё состояние
(`state_multi.json`), раздельная статистика (`version='multi'` в БД).
**Без трансляции в канал** — прогресс только в админке.

Логика входа идентична scalp v2, но применяется параллельно к нескольким инструментам:
частота набирается диверсификацией, а не ослаблением фильтров.

### Отбор инструментов (скан всех символов брокера, 2026-08-25)
Критерии: стоп на мин. лоте ≤ 2.5% от $100 **и** спред ≤ 30% от ATR(M5).

**Прошли (6):** NAS100.s, GER40.s, UK100.s, USOUSD.s (нефть), UKOUSD.s, BTCUSD — стоп $0.07–2.43 (0.1–2.4% от $100).

**Отсеяны:**
| Инструмент | Причина |
|---|---|
| XAUUSD, XAGUSD | стоп 4.8% / 6.1% от $100 при мин. лоте — дорого для микросчёта |
| EURUSD, GBPUSD, AUDUSD | спред съедает 65–100% ATR(M5) — **опровергло прежнюю гипотезу «мажоры лучше для мини-баланса»** |
| SOLUSD, XRPUSD, LTCUSD, ETHUSD | спред 180–750% от ATR |
| HK50, EU50, FRA40 | спред 37–52% от ATR |

### Параметры
Лот 0.01 · макс. 3 позиции всего и 1 на инструмент · 45 сделок/день суммарно ·
кулдаун 3 мин на символ · окно 07:00–19:00 UTC · серия 3 стопов → пауза инструмента до след. дня.

**Риск-фильтр (постоянный):** перед входом считается стоимость стопа; если > 2.5% от
тестовой базы $100 — сделка пропускается, причина видна в админке. Защита от скачков волатильности.
Дневной лимит убытка считается от **тестовой базы $100**, а не от демо-счёта.

**Статус (27.08):** 32 закрытых сделки, WR 25%, **−$3.16** — медленно спускает на спреде.
Прогноз 15–35 сделок/день не оправдался (факт 9-12). На live не идёт; остаётся на демо —
добрать статистику и отсечь убыточные инструменты/часы по аналитике входов.

---

## Стратегия 5: Скальпер V3 (main_scalp3.py) — ЭКСПЕРИМЕНТАЛЬНЫЙ

ML-скальпер на золоте: XGBoost (48 признаков, confidence-фильтр ≥0.6) + теханализ +
детекция режима рынка (trending/ranging/volatility). Модель обучена на **15 000 реальных
баров M5** из MT5, accuracy 48.6% (честно: уровень монетки, значимого edge нет).

| Параметр | Значение |
|---|---|
| Инструмент | XAUUSD.s, лот 0.01 |
| TP/SL | 7/5 пипсов от kiro-конфига; стоп ограничен 4% от базы $100 |
| Лимиты | 12 сделок/день, max 2 позиции, кулдаун 5 мин |
| Стопы | дневной -6% от базы, окно 06:00–20:00 UTC, выходные — пауза |

Внешние данные (все реальные, проверены вживую): RSS-новости (investing/marketwatch/kitco),
Fear&Greed с alternative.me, VIX/DXY через yfinance, экономкалендарь Forex Factory JSON,
имбаланс по тикам MT5, TradingView cfd/scan multiframe.
**Позиционирование: 5-й экспериментальный бот для параллельного сравнения, НЕ основной.**
**Статус (27.08):** 12 сделок, WR 8.3% (1 прибыльная из 12), **−$6.51** — весь минус за сегодня. Edge не виден.
⚠️ Имя внешнего источника нигде в интерфейсе/канале не упоминается — в админке бот
подписан «⚡ СКАЛЬПЕР V3», в БД метка `version='kiro'` (техническая).

---

## 🖥️ Миграция на VPS заказчика (2026-08-28) — СТАТУС: ✅ 100% рабочее (порты открыты, Telegram через релей, гибрид торгует). Осталась только смена secrets.

**VPS:** REG.RU `185.46.10.179`, Windows Server 2016 (build 14393), вход Administrator.
Домен **fxbot.space** (REG.RU). Открытые порты: только RDP 3389 (SSH/WinRM закрыты —
управление через RDP, автологин из `vps.rdp` + cmdkey, файл в .gitignore).

### Что сделано
| Шаг | Детали |
|---|---|
| Код | Проект → `C:\forexbot` через RDP-проброс диска (`\\tsclient\E`), robocopy 36 папок / 88 файлов / 0 сбоев; исключены .git/.venv/backup/downloads/логи. Репо на GitHub синхронизировано (fastlane: merge -X ignore-all-space после веб-заливки заказчика) |
| Данные | `bot.db` + `state*.json` + `telegram.json` скопированы — статистика непрерывна |
| Python | 3.12.10 тихой установкой (`/quiet InstallAllUsers=1 PrependPath=1`), venv `C:\forexbot\.venv`, пакеты: MetaTrader5 numpy pandas fastapi uvicorn matplotlib openpyxl requests — импорты ОК |
| MT5 | Терминал скопирован с ПК в **тот же путь** `E:\AI\MetaTrader5` (путь задаёт хэш папки данных — поэтому без правок конфигов). Только terminal64.exe 117 МБ + Sounds/Profiles/Config (Bases/Editor/тестер не нужны). Конфиг счёта (`_mt5data/` = accounts.dat и пр., 486 КБ) → `%APPDATA%\MetaQuotes\Terminal\F0630FA0...\config` |
| Логин | Пароль MT5 не переносится между машинами — введён в диалоге вручную, «Сохранить пароль» включён. Проверка python: `INIT True`, счёт 700158875, **баланс $100 410.25**, `trade_allowed True` |
| Боты | **Гибрид (`main_hybrid run`) + веб-панель + `main run` (halted, только Telegram-команды) запущены на VPS.** Автозапуск ONSTART: `fxbot-terminal` → `fxbot-hybrid` (+1мин) → `fxbot-web` (+1.5мин) → `fxbot-telegram` (+2мин). Локальные боты остановлены (двойной поллинг = конфликт) |
| Интернет VPS | Работает через прокси REG.RU; python.org/github/pypi — ок; **PU Prime демо-сервер доступен с RU IP без VPN** |
| DNS | `fxbot.space` и `admin.fxbot.space` → 185.46.10.179 — **разошлись** (проверено nslookup через 8.8.8.8) |

### Что осталось (блокеры и очередь)

**Блокер 1 — ПОРТЫ (REG.RU).** Входящие 80/443/8181 дропаются на границе REG.RU:
независимая проверка check-host → «Connection timed out»; на самом VPS при этом
публичный IP на NIC (NAT нет), порты слушают, правила Windows-файрвола активны.
Тикет в поддержку REG.RU открыт (юзер). В панели ЛК раздел портов не найден.
После открытия: `C:\caddy\caddy.exe run --config C:\caddy\Caddyfile` (скачан,
Caddyfile на оба домена готов) → https://fxbot.space + admin.fxbot.space с
авто-HTTPS Let's Encrypt. План Б при затягивании: Cloudflare Tunnel (cloudflared).

**Блокер 2 — TELEGRAM 409.** api.telegram.org с RU IP заблокирован РКН
(`TG_API:000` с VPS при работающем с локалки через VPN). **Патч перенесён из
крипто-бота (trade):** `_pin_telegram_dns()` в `notifier.py` + `TELEGRAM_API_IPS`
в `config.py`/`.env` (пиннинг живых IP 149.154.167.220 / .175.50 / .166.110,
SNI валидный). Задеплоено на сервер, процесс перезапущен — в логе
«telegram DNS pinned», ручной `getMe` через IP → `{"ok":true}` (транспорт работает).
НО поллинг ловит **HTTP 409 Conflict** — где-то ещё есть второй consumer getUpdates
того же токена. Проверено: локальная машина чиста (процессов/задач нет), вебхук
не установлен (`getWebhookInfo` → url:"", pending_update_count=4). Главный
кандидат — крипто-бот (aliter230880/trade) на другом VPS с тем же токеном.
**Следующий шаг (разбор остановлен по указанию юзера):** найти второй поллер
или выпустить новый токен бота и заменить в `.env` VPS.

**Очередь после блокеров:** старт Caddy → HTTPS; копитрейдинг dry-run на VPS
(`COPIER_DRY_RUN=1`, тот же терминал); 2-й терминал при переходе на live;
⚠️ сменить пароль Administrator VPS (светился в переписке) и пароль VNC/RDP там же.

### Грабли, найденные при миграции
- **VPN на ПК юзера мешает RDP** к REG.RU — на время работ отключать (юзер его
  переключает по своим делам — RDP-сессия рвётся, но удалённая сессия на VPS
  живёт; набранный-но-не-отправленный текст в cmd переживает реконнект)
- ПК юзера перезагружался посреди работ — все PID локальных процессов сменились
- Синтетические клики в secure-UI логина RDP не проходят — работает только
  клавиатурный канал (клавиши mstsc пересылает в сессию)
- **Фокус на локальной машине постоянно перехватывается** (юзер работает
  параллельно) — команды в RDP вводить короткими пачками; всё длинное —
  скриптом-файлом через `\\tsclient` и запускать одной строкой
- Установщики PU Prime на CDN MetaQuotes по угадываемым URL = 404; терминал
  проще скопировать целиком (117 МБ достаточно)
- MT5 не переносит сохранённый пароль между машинами (привязка к машине) —
  на новом хосте логин руками один раз, «Сохранить пароль» обязательно
- **api.telegram.org заблокирован РКН**: с VPS таймаут; лечится пиннингом IP
  (см. блокер 2); НО если у бота есть второй поллер — 409 Conflict поверх всего
- В панели REG.RU ЛК нет раздела портов/файрвола VPS — только через поддержку
  (или это их граничная фильтрация без настройки)



## Копитрейдинг — ГОТОВ, dry-run пройден, СЛЕДУЮЩИЙ ШАГ: сервер заказчика

Файлы: **`copier_engine.py`** (движок) + **`main_copier.py`** (раннер run/status) +
таблица **`copy_trades`** в bot.db + состояние `data/state_copier.json`.

**Архитектура:** мастера (гибрид и др.) пишут сделки в общую bot.db → копир-процесс
читает их и зеркалит на ВТОРОМ терминале MT5 (счёт подписчика). Ни один мастер
не знает о копире — отвязка полная. При переносе на сервер заказчика мастера и
копир живут на одной машине — общая bot.db, без сетевого доступа.

| Механика | Реализация |
|---|---|
| Лот | баланс_подписчика / баланс_мастера × лот_мастера → нормализация к шагу, потолок MAX_LOT; COPIER_FIXED_LOT>0 — фикс. лот без масштабирования |
| Вход | рыночный при появлении сделки мастера; SL/TP переносятся от мастера (уровни абсолютные), раздвигаются до мин. дистанции stops_level + 1.5 спреда |
| Закрытие | мастер закрылся (tp/sl/manual) → копир закрывает зеркалку по рынку, pnl обеих сторон пишется в copy_trades |
| Свежесть | копируются сделки не старше 300 сек; на первом старте last_master_id = MAX(id) — накопленную историю не копируем |
| Лимиты | макс. зеркалок одновременно (6), макс. лот (0.1), повторное зеркалирование того же тикета исключено (already_copied) |
| Filling | фолбэк IOC → FOK → RETURN (как у остальных ботов) |

**Конфиг (всё в .env, дефолты в config.py):**

| Переменная | Дефолт | Смысл |
|---|---|---|
| COPIER_DRY_RUN | 1 | 1 = только логи и БД, ордера НЕ отправляются |
| COPIER_MASTERS | hybrid,kiro | версии-мастера из scalp_trades |
| COPIER_LOGIN / PASSWORD / SERVER | 0 / — / — | счёт подписчика (0 = текущий счёт терминала) |
| COPIER_MT5_PATH | E:\AI\MetaTrader5\terminal64.exe | путь ко 2-му терминалу |
| COPIER_MASTER_BALANCE | 100000 | база для масштабирования лота = баланс мастера |
| COPIER_FIXED_LOT / COPIER_MAX_LOT | 0 / 0.1 | фикс. лот / потолок лота |
| COPIER_MAX_POS / COPIER_MAX_LATENCY_SEC | 6 / 300 | лимит зеркалок / свежесть сделок |
| COPIER_POLL_SEC | 5 | период опроса БД |
| COPIER_MAGIC | 20260828 | magic копира |

**Тест 2026-08-27 (dry-run, демо, локально):** копир увидел 2 открытые сделки
гибрида (GER40.s LONG, NAS100.s LONG), залогировал зеркальные входы с SL/TP,
маппинг записан в copy_trades (виртуальные тикеты −1), last_master_id=126.
Ордера НЕ отправлялись.

**Чек-лист подключения на сервере заказчика:**
1. Windows VPS 2 vCPU / 3-4 ГБ / 40 ГБ + **второй** терминал MT5 в отдельной папке данных (терминал одноэкземплярный на папку!)
2. Перенести проект (git clone private-репо) + .env; счёт подписчика: COPIER_LOGIN/PASSWORD/SERVER
3. COPIER_MT5_PATH → путь ко 2-му терминалу; masters и копир на одной машине — общая bot.db
4. Прогнать ≥1 дня с COPIER_DRY_RUN=1 на сервере: копир видит сделки мастеров, copy_trades наполняется
5. COPIER_MASTER_BALANCE = реальный баланс мастер-счёта (от него считается лот)
6. COPIER_DRY_RUN=0 → `python -m backend.main_copier run`; контроль: `status`, `data/copier.log`
7. Первый день сверять pnl мастер/копия в copy_trades — масштаб лота и проскальзывание в норме

---

## Сигналы в канал @forex_vip_first

| Тип | Содержание |
|-----|-----------|
| 📊 СЕТКА (собрана/сдвинута) | уровни, шаг, лот + **ATR-прогноз** времени + **график зоны торговли картинкой** |
| 🟢 УРОВЕНЬ СРАБОТАЛ | цена buy, TP, ETA до тейка, как повторить |
| ✅/🔻 ПАРА ЗАКРЫТА | PnL + итог нарастающим итогом |
| ⚠️ тренд / стоп / weekend | защитные события |
| ⚡ СКАЛЬП (под флагом) | вход, TP, SL, тренд H1, ADX |
| 🟢/🔴 ГИБРИД ВХОД (29.08) | инструмент, направление, цена, TP/SL, тренд H1 — с тегом [ДЕМО]/[LIVE] |
| ✅/🔻 ГИБРИД ЗАКРЫТ (29.08) | причина (тейк/стоп), PnL, winrate, итого — с тегом [ДЕМО]/[LIVE] |

**Трансляция ГИБРИДА в канал ВКЛЮЧЕНА (29.08)** — `send_signal()` в notifier, вызывается
из `hybrid_engine.sync()` на FILL и CLOSED. Управление: `.env` `HYBRID_CHANNEL_SIGNALS`
(вкл/выкл всё), `HYBRID_SIGNAL_FILLS` (входы), `HYBRID_SIGNAL_CLOSES` (закрытия) — все дефолт 1.
Тег `[ДЕМО]`/`[LIVE]` берётся из `TRADING_MODE`. Это основная витрина канала теперь.

**Трансляция скальпа выключена** (`scalp_signals: false` в state) — включается кнопкой
в админке. Мульти-бот в канал не пишет вообще. Критичные стопы всегда уходят владельцу в личку.

**Закреплённый пост канала:** «Как читать сигналы · Правила микро-счёта» — типы сигналов,
как повторять (лимитки или вход по рынку на 🟢), для $100-150: только 2-3 ближних уровня,
лот 0.01, стоп -15%, без переноса через выходные, дисклеймер.

---

## Telegram-бот @Dim_forex_bot (демо: открытый доступ)

Любой написавший → авто-подписка на уведомления + доступ к командам:
- `/status` — сводка (работает и при выключенном терминале)
- `/start` — возобновить после стопа (сам поднимает терминал и сетку)
- `/stop` — **полный стоп**: ордера + позиции + закрытие терминала
- `/help` — команды

Команды обрабатываются в отдельном try — краши торгового цикла их не блокируют.
chat_id подписчиков в `data/telegram.json` (не в state.json — его перезаписывает цикл).

---

## Веб-панели (:8181)

**Публичная `/`** — «страница доверия»: баннер «данные с торгового сервера, сигналы 1-к-1»,
PnL, equity-график, winrate, история пар, ссылка на канал (ссылки на код нет).

**Админка `/admin`** (BasicAuth):
- Управление: полный стоп / запуск (через `data/cmd.json`, исполняет сам цикл бота — без гонок)
- Рассылка подписчикам
- Блок **мульти-бота**: статус, сделки, winrate, PnL, риск-база, результаты по инструментам, открытые/закрытые сделки с ADX, свои кнопки стоп/старт (`cmd_multi.json`)
- Блок **скальпера**: статистика, переключатель трансляции в канал, таблицы сделок
- Счёт: баланс, маржа, margin level, тренд, затраты
- Equity-график, подписчики, лента событий

---

## Аналитика входов

В `scalp_trades` для каждой сделки пишется контекст: **adx, atr, h1_trend, hour_utc,
spread, ema_gap, max_favor, version, symbol**. Через 30-50 сделок можно увидеть, при каком
ADX и в какие часы стратегия зарабатывает, и отсечь остальное на данных, а не на догадках.

---

## Известные грабли (важно!)

1. **IPC timeout** (библиотека ↔ терминал): лечится чистой папкой данных + логином в GUI (кэш сервера). После переустановки: `Файл → Открыть счёт → поиск PUPrime`.
2. **«AutoTrading disabled (10027)»**: галка «Разрешить алгоритмическую торговлю» применяется только **на живом сеансе** (Ctrl+O → Советники). После нового старта терминала проверять `trade_allowed`.
3. Терминал **одноэкземплярный** на папку данных; библиотека спавнит свой — перед ручными действиями убивать все terminal64.
4. **Прямые правки `state.json` при живом боте бесполезны** — цикл затрёт. Только через `/start`, `/stop` или при остановленном процессе.
5. У отложенных ордеров MT5 **нет привязки к позиции** (position_id=0) — поэтому только серверные TP.
6. Терминал от MetaQuotes-инсталлера может прийти бетой (6120/6122) — с пакетом 5.0.6090 работает.
7. **Фоновые процессы из Git Bash умирают с сессией** — запускать только через `start_bot.ps1`.
8. Автозамены в коде скриптом с `str.replace` **молча не применяются**, если шаблон не совпал (так три из четырёх заглушек скальп-сигналов не встали) — проверять `grep` после правки.
9. Порт 8080 занят чужим `python -m http.server` — используем 8181.

---

## Результаты (демо)

| Дата | Событие |
|------|---------|
| 2026-08-19 | Первый запуск сетки (12 уровней, шаг $6, лот 0.01) |
| 2026-08-20 | Переход на серверные TP; +$283/день; параметры 15×$4/лот 0.02 |
| 2026-08-21 | Сетка: 23 пары, **+$381**; weekend flat отработал |
| 2026-08-22 | Канал подключён; weekend flat → 23:00 UTC; сетка **+$413 / 27 пар** |
| 2026-08-24 | Скальп v1 запущен → провал (WR 30.5%, -$28.10), остановлен |
| 2026-08-25 | Скальп **v2** (H1+ADX+M5+трейлинг, бэктест WR 53.7%) + **мульти-бот** на 6 инструментах (первая сделка +$4.94) |
| 2026-08-26 | **Скальпер V3** (ML, стратегия №4) добавлен 4-м в админку; ML-модель обучена на 15k баров (acc 48.6%); кнопки на рабочем столе; 4 бота в отдельных рамках |
| 2026-08-27 | Excel-отчёт (`report_excel.py`: сводка / по дням / все сделки, +колонка PnL за сегодня). Итоги: **гибрид +$47.55 WR 86.2% — лидер, на live идёт только он**; v2 чистый +$3.65; мульти −$3.16; V3 −$6.51. Копитрейдинг прошёл dry-run (2 сделки гибрида зеркалены виртуально) |
| 2026-08-31 | **GO-LIVE гибрида на реальном счёте 29399422 / PUPrime-Live ($150)**. Файлы на VPS доставлены paste-скриптами `_server/update_live_files.ps1` + `update_env_live.ps1` (git на сервере нет; `live\` = 6 файлов, sha256-проверки). `go_live.ps1` (версия без git pull): стоп ботов → `.env` live (base 150, TRADING_MODE=real, TERMINAL_PATH=C:\MetaTrader5) → демо-БД в архив `data\archive\bot_demo_20260831_*.db`, чистая БД → патч TG-owner (`/stop`//`start` только владелец 789368186) → рестарт. **Гоча №1:** бот сам логинит терминал по кредам из `.env` (`mt5_gateway.connect`) — ручной вход в MT5 перезаписывается; **Гоча №2:** `day_anchor` в `state_hybrid.json` хранил демо-итог дня (+$53.30), после очистки БД дал ложный «дневной лимит −35.5%» HALT — лечится сбросом `day_anchor`/`halted` + рестартом задачи. 12:45 первая live-сетка XAUUSD.s. Символы на live те же (`*.s`). ⚠️ НЕ сделано: пароль 29399422 (светился в чате 2×), пароль RDP, перевыпуск TG-токена. Копир ждёт счёт «трейдера» (COPIER_DRY_RUN=1) |

**Итог по стратегиям (27.08):** сетка +$503.79 (35 пар) · скальп v1 −$28.10 · скальп v2 чистый +$3.65 · **гибрид +$47.55** · мульти −$3.16 · V3 −$6.51.

---

## ⚠️ Про целевую доходность (проговорено с заказчиком)

Запрос: **8–11%/день**. Честная оценка: **недостижимо** легальной стратегией.
Арифметика на $100 при лоте 0.01: победа ≈ +3%, проигрыш ≈ -2%. Даже при WR 55-60%
реалистичный результат — **1.5–3%/день в среднем, с красными днями**.
Гнаться за 10% можно только увеличением лота → микросчёт превращается в лотерею.

Также: «максимум сделок + максимум прибыли + мини-баланс» — взаимоисключающие требования.
Правильный обход — не частить одним инструментом, а торговать много инструментов
качественной логикой (что и делает мульти-бот).

---

## Дорожная карта

1. **Набрать статистику** v2 (золото) и мульти (6 инструментов) — 30-50 сделок каждый
2. Разбор аналитики входов → отсечь убыточные условия (ADX/часы/инструменты) на данных
3. **Копитрейдинг на сервере заказчика** — следующий шаг, чек-лист в разделе выше
4. **Windows VPS** (~$15/мес) + автозапуск при ребуте → 24/5 без ПК юзера (мастера и копир на одной машине)
5. **Live $100** мастер (гибрид) + $100 копитрейдер, тест 1-2 недели → отчёт «сколько реально заработал»
6. Тарифы канала: free / платный приватный (Telegram Stars)

### Что нужно от заказчика
- **Windows VPS** 2 vCPU / 3-4 ГБ RAM / 40 ГБ SSD — $12-20/мес (MT5 только под Windows)
- Депозиты для live-теста: $100 мастер + $100 копир
- Домен (опционально, для публичной страницы) — ~$1/мес
- **Итого фиксировано: ~$15-20/мес**
